namespace Chizl.FileCompare
{
    public enum DiffType
    {
        None,
        Added,
        Deleted,
        Modified
    }
    public enum FileFormat
    {
        NotSet,
        Ascii,
        Binary,
    }
}
